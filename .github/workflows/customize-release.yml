name: Customize Fork Release (Deprecated)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy repack flow
        run: |
          echo "Legacy customize-release flow is disabled."
          echo "Reason: upstream asset repack keeps Camoufox branding in binary resources."
          echo "Use build.yml (source build) to publish true Bugox-branded binaries."
          exit 1
